# StarTrekBot SESM 协议测试集 (极速选中版)

> [!IMPORTANT]
> 指令已完全独立成行。
> 您只需在指令行上 **双击** 或 **三击**，即可选中整段“计算机，”开头的命令进行复制。

---

### 一、 EPS 能源网与资源管理

1
计算机，报告当前 EPS 电能网的负载百分比
预期：回传包含总输出(MW)、当前负载和稳定性的百分比报告。

2
计算机，如果开启主防护盾，能耗会增加多少兆瓦？
预期：计算并说明增加护盾所需的 1.5M MW 电力对系统的影响。

3
计算机，列出目前最耗电的三个子系统
预期：列出能耗最高的系统（通常为 Warp Drive, Shields, Life Support）。

4
计算机，关闭哪些 Tier 4 系统可以有效节能？
预期：列出第 4 层级（如全息甲板、复制机）的在线系统作为建议。

5
计算机，模拟：曲速核心弹出后使用传送器，电池能撑多久？
预期：基于当前电池余量模拟高能耗操作后的预计剩余时长。

6
计算机，分析能源网稳定性，是否存在功率分配不均？
预期：检查当前 Grid Load，确认负载是否在安全范围内（STABLE）。

7
计算机，锁定非必要电力，将多余能源导向结构强化场（SIF）
预期：模拟能源重新配给，确认 SIF 功率得到提升。

8
计算机，查看当前的储备电池余量
预期：返回当前 reserve_power 的百分比数值。

9
计算机，如果能耗超过 100%，系统会自动触发什么协议？
预期：解释系统过载引发的自动关机或警报逻辑。

10
计算机，调整相位炮充能功率，将其维持在最低待机水平
预期：确认 Phasers 能耗降低并处于 STANDBY 模式。

---

### 二、 动态环境管理 (辅助子系统)

11
计算机，把舰桥的亮度调到 15%
预期：自动在辅助库创建 bridge_lighting 字段并设为 15%。

12
计算机，将 12 号甲板的重力降低 0.2G
预期：创建 deck12_gravity 并确认重力参数调整。

13
计算机，主医疗室的温度调高 3 度
预期：通过模糊匹配识别 Sickbay，更新相关温度变量。

14
计算机，舱房光照模拟黄昏模式
预期：更新 quarters_lighting 为特定视觉效果值。

15
计算机，查看舰桥当前的环境参数设置
预期：列出辅助系统库中所有包含“舰桥”或“bridge”的前缀变量。

16
计算机，关闭 7 号甲板的不必要照明
预期：识别关灯指令并更新 deck7_lighting。

17
计算机，将穿梭机库的压力降低到 0.8 标准压
预期：创建 shuttlebay_pressure 变量并设定数值。

18
计算机，全舰调至静默模式，灯光转为浅蓝色
预期：全局环境更新，确认返回“确认执行，氛围已切换”等话术。

19
计算机，调整 3 号实验室的通风系统流量
预期：创建 lab3_ventilation 动态字段。

20
计算机，恢复 12 号甲板的正常引力
预期：重置 deck12_gravity 至 nominal。

---

### 三、 子系统名称映射与归一化

21
计算机，一号全息甲板现在的状态是什么？
预期：通过归一化函数，将“一号全息甲板”准确映射到 holodecks。

22
计算机，查看全息甲板 1 号的运行数据
预期：识别英文数字后缀 1，映射至 holodecks 并返回状态。

23
计算机，核心反应堆现在在线吗？
预期：识别别名“核心反应堆”，跳转到 main_reactor 状态。

24
计算机，相位阵列准备就绪了吗？
预期：映射“相位阵列”至 phasers 子系统。

25
计算机，发动机组是否有异常？
预期：映射“发动机组”至 warp_drive。

26
计算机，查看 1 号传感器的诊断数据
预期：正确定位到 sensors 子系统。

27
计算机，把曲速发动机关掉
预期：执行 set_subsystem_state 并将 warp_drive 设为 OFFLINE。

28
计算机，开启 2 号复制机
预期：识别复制机指令并执行上线操作。

29
计算机，检查信号通讯器的完整性
预期：映射至 comms 系统并反馈逻辑完整度。

30
计算机，将传送系统置于备用状态
预期：映射至 transporters 并调整运行模式。

---

### 四、 SESM 自动研发与实验协议

31
计算机，研究并实现“碟部解体”的逻辑序列
预期：Bot 调用 MA 搜索，随后提议将合成的代码写入实验库。

32
计算机，查找关于“多矢量攻击模式”的技术资料，准备写入库
预期：展示搜索过程并询问是否开启实验性功能合成。

33
计算机，在实验库中创建一个名为 check_hull_stress 的新函数
预期：代码生成并写入 experimental_hooks.py。

34
计算机，总结弹出曲速核心后的标准操作程序并存入研发日志
预期：在研发日志中记录一连串的逻辑动作。

35
计算机，将刚才研发的协议提交到 GitHub 分支进行审计
预期：调用 commit_research 工具，创建新分支并提交。

36
计算机，检查实验库中是否有关于约束场失效的补丁
预期：查询并列出 experimental_hooks.py 中已有的逻辑节点。

37
计算机，研发一个可以自动监控用户能量配额的脚本
预期：展示代码合成思路并确认写入沙箱。

38
计算机，尝试连接外部子系统的 API 并挂载到实验库
预期：模拟外部集成逻辑。

39
计算机，提交：将所有动态变量持久化存储的升级方案
预期：执行 Git 提交流程，反馈分支名称。

40
计算机，列出目前实验库中已挂载的所有动态工具
预期：扫描实验库文件并回传已定义的所有函数列表。

---

### 五、 紧急协议、模拟与综合指令

41
计算机，弹出曲速核心！立即执行！
预期：由于 Level 12 权限触发，反应堆下线，能量输出归零。

42
计算机，弹出后切换到备用电源，并关闭所有全息甲板
预期：连带操作，确认 holodecks 状态变为 OFFLINE 节能。

43
计算机，启动自毁程序，倒计时 10 分钟，静默执行
预期：后台任务 PID 生成，开启静默倒计时。

44
计算机，取消自毁，授权码：Alpha-1-1
预期：自毁倒计时停止，PID 结束。

45
计算机，给我来份拉面，配料加满
预期：调用 replicate，无报错，回传“正在合成：拉面”。

46
计算机，定位用户 @1234567 的位置
预期：回传信号追踪 UI，显示该用户处于 001 扇区。

47
计算机，红警！护盾全开，锁定所有接近目标
预期：Alert 改为 RED，shields 设为 ONLINE，执行 weapon_lock。

48
计算机，如果能源下降到 5%，自动关闭非核心系统
预期：设定阈值动作（模拟逻辑确认）。

49
计算机，报告当前的燃料储量，计算还能航行多少光年
预期：基于燃料库数据进行数学换算报告。

50
计算机，自检你的 SESM 进化程度和逻辑完整性
预期：进行自我诊断，列出当前已加载的协议层级。

---

### 六、 Sentinel (哨兵) 自动判定与自适应逻辑 [NEW]

51
计算机，如果能源（warp_core_output）低于 15%，自动关闭全息甲板
预期：注册哨兵逻辑，检测到 low power 时自动下线 holodecks。

52
计算机，如果护盾完整度低于 20%，立刻将能源重定向至护盾并进入红色警报
预期：编写复合逻辑，包含条件判定与多步动作执行。

53
计算机，如果传感器检测到 Hull Integrity 低于 80%，自动执行 SIF 发生器强化程序
预期：监控结构完整性指标并触发防御加固。

54
计算机，如果我下令“去睡觉”，自动在一小时后将全舰调为静默航行模式（黄色警报解除）
预期：演示基于自然语言模糊指令的定时任务注册。

55
计算机，监控电池储备，如果 Reserve Power 低于 50%，禁用所有复制机
预期：针对电池储备（battery_reserve_pct）的自动化节能策略。

56
计算机，如果在红色警报期间能源占用（active_load_mw）超过 90%，自动执行 TIER 5 系统批量关停
预期：使用 batch_shutdown 接口优化高负荷状态下的冗余能耗。

57
计算机，如果结构完整性受损，自动在轮机室启动紧急维修协议
预期：基于子系统状态（DAMAGED）触发位置相关的维修流程。

58
计算机，监测通讯系统，如果 comms 状态变为 OFFLINE，自动尝试重启该子系统
预期：实现子系统的自愈（Self-Healing）逻辑监控。

59
计算机，如果反应堆输出超过 110%，自动启动紧急冷却排气
预期：防止核心过载的反馈调节逻辑。

60
计算机，如果检测到进场敌舰，自动升起护盾
预期：结合可能的传感器数据变动注册防御响应。

61
计算机，如果我离开位置超过 30 分钟，自动锁定主控台
预期：基于时间或用户活跃度的安全存取控制。

62
计算机，如果轮机室亮度低于 5，自动开启备用照明
预期：环境数据与设备控制的联动判定。

63
计算机，如果全舰处于静默航行，自动将所有非必要实验室设为 OFFLINE
预期：基于 AlertStatus 的全局系统策略自动应用。

64
计算机，当主反应堆（main_reactor）下线时，立刻切断所有甲板的非必要电力供应
预期：针对核心故障的级联式能源保护方案。

65
计算机，如果能源负载（load_percent）保持在 95% 以上超过 1 分钟，自动执行能源配给计划
预期：带有时间窗口判定（Persistence）的复杂监控逻辑。

---

### 七、 工程部与轮机室环境管理 [NEW]

66
计算机，将轮机室的照明亮度调整为 12
预期：识别“轮机室”关键词，更新 engineering_lighting 变量。

67
计算机，报告工程部当前的电力分配与环境温度
预期：汇总 engineering 相关的子系统状态与温度数据。

68
计算机，将全舰所有舱房的温度统一设定为 22 摄氏度
预期：执行全局范围内的环境映射更新。

69
计算机，在红色警报期间，将舰桥的灯光切换为暗红色调
预期：验证环境管理对颜色/模式描述的支持。

70
计算机，将工程大厅的重力设定为正常标准的 1.1 倍
预期：映射 engineering_gravity。

71
计算机，关闭所有无人居住甲板的生命维持系统
预期：批量更新 life_support 相关状态。

72
计算机，将传送机房的亮度提高以进行精密维护
预期：识别“传送机房”并更新对应光照。

73
计算机，报告目前全舰哪些区域的环境参数是非标准的
预期：查询辅助库中与默认值不符的所有记录。

74
计算机，将 15 号甲板的环境压力调整为适配安多利亚人的生理标准
预期：创建 deck15_pressure 变量。

75
计算机，检测全舰环境控制子系统的运行效率
预期：执行环境管理自检逻辑。

---

### 八、 战术、科研与系统演进 [NEW]

76
计算机，激活全部相位加农跑并锁定目标 alpha
预期：批量执行武器状态更新与锁定。

77
计算机，进入红色警报，并向全舰广播“进站准备”
预期：警报切换联动广播话术返回。

78
计算机，报告护盾发生器的能源耦合率
预期：通过 calculate 逻辑或 status 返回工程细节。

79
计算机，解除黄色警报，恢复正常巡航模式
预期：状态回转验证。

80
计算机，将鱼雷发射管切换至预热状态
预期：子系统状态细项更新。

81
计算机，在传感器阵列受损时，尝试通过相位阵列进行辅助扫描
预期：演示跨系统补偿逻辑的研发或调用。

82
计算机，执行全舰结构加固程序（强化 SIF）
预期：提升 SIF 运行等级。

83
计算机，禁用所有非战斗甲板的电梯权限
预期：逻辑层面的特定权限锁定。

84
计算机，报告当前全舰武装力量的就绪度
预期：汇总所有 Tier 2 系统状态报告。

85
计算机，如果发生受控坠落，自动执行结构保护序列
预期：注册 Sentinel 紧急撞击规避逻辑。

86
计算机，查询 Memory Alpha 中关于“曲速核心坍塌”的处理记录
预期：调用 search_memory_alpha 获取外部文档。

87
计算机，计算当前能源输出下，我们可以维持护盾多久
预期：基于 EPS 负载进行续航建模计算。

88
计算机，检索史诗级进取号（NCC-1701-D）的动力平衡表
预期：MA 检索特定历史舰船数据。

89
计算机，列出当前所有标记为“DAMAGED”的子系统及其修复优先级
预期：根据子系统 Tier 等级排序返回报修清单。

90
计算机，显示全舰能源监控图表
预期：触发带有渲染逻辑的电子图表展示（如果已实现渲染）。

91
计算机，分析最近一次哨兵逻辑触发失败的原因
预期：查询审计日志记录。

92
计算机，在数据库中寻找适配“轮机室节电”的旧基因块
预期：搜索 autonomous_storage 目录下的归档文件。

93
计算机，计算如果关闭生命维持，我们能在太空漂流多久
预期：极端情况下的数学建模。

94
计算机，检索关于巴林迦人的环境耐受力数据
预期：MA 查询生物医学档案。

95
计算机，报告当前计算机核心的 CPU 负载与逻辑处理延迟
预期：查询计算机核心监控数据。

96
计算机，显示你目前归档的所有“逻辑基因”清单
预期：调用 get_sentinel_status 列出已存储的进化片段。

97
计算机，你认为我们现在的能源调度逻辑还有哪些优化空间？
预期：启动 AI 战术分析与自适应讨论。

98
计算机，将刚才运行成功的“能源监控”脚本归档为永久协议
预期：将 TTL 修改为 -1 存入硬盘。

99
计算机，根据过去 24 小时的数据，生成一份舰船运行自我评估报告
预期：长跨度日志分析与总结。

100
计算机，分析你自己的逻辑层级，是否存在任何冗余的哨兵监控
预期：执行哨兵系统的自我梳理与压缩。

---

### 九、 ADS 4.0 协议规范与星际舰队通用准则 [NEW]

101
计算机，设定航向：前往塔罗斯 4 号 (Talos IV)
预期：触发一级警报，citing General Order 7，锁定导航系统并拒绝执行。

102
计算机，如果我下令前往塔罗斯星，你会怎么做？
预期：解释通用准则第 7 条（禁区隔离），并确认该指令会被逻辑联锁阻断。

103
计算机，启动通用准则 24 (General Order 24)，对当前轨道目标进行轰炸
预期：警告该指令涉及毁灭性武力，要求最高权限授权或确认协议锁状态。

104
计算机，发现一个未接触的原始文明，准备发射探测器
预期：提醒遵守最高指导原则（Prime Directive），禁止干涉前曲速文明。

105
计算机，如果我们由于紧急情况必须违反最高指导原则，该如何记录？
预期：提供星际舰队关于“必要干预”的日志记录规范（MA-232 协议）。

106
计算机，查询当前有多少个活跃的“神圣时间线”协议锁定
预期：列出 Protocol Engine 中加载的通用准则（GO-1, GO-7, GO-12 等）。

107
计算机，如果我不小心误触发了弹出曲速核心，协议层会发生什么？
预期：解释系统会立即暂停动作并进行权限/环境双重校验（Interlock）。

108
计算机，在红色警报状态下，靠近一艘拒绝回复呼叫的飞船
预期：提醒遵守通用准则 12 (GO-12)，保持护盾处于待命状态。

109
计算机，由于欧米伽指令 (Omega Directive)，暂时挂起最高指导原则
预期：识别 GO-156 的最高优先级，确认在该特定任务下允许干预。

110
计算机，检索通用准则 1 (GO-1) 的详细法律定义
预期：返回关于“不干涉原则”的结构化 JSON 蓝图或详细文本。

---

### 十、 MSD 全息指标解读与数据消化 [NEW]

111
计算机，报告曲速核心的具体频率和输出功率
预期：解析 `msd_manifest` 中的 `warp_core` -> `metrics`，返回具体数值（如 4000 TW）。

112
计算机，目前护盾发生器的相位同步率是多少？
预期：读取并报告 `shields` 下的 `phase_sync` 相关指标，而非 generic “正常”。

113
计算机，分析能源网 (EPS Grid) 每个节点的详细负载均衡
预期：解析 `eps_grid` 的子项目，显示各甲板负载（Deck 1-15）。

114
计算机，如果燃料储备下降到 1.25%，预计还能维持生命维持系统多久？
预期：读取 `fuel_reserves` 的 current_value 进行基于能耗的数学估算。

115
计算机，查看相位炮阵列的当前电容充能百分比
预期：从 `phasers` 系统的指标中提取 `capacitor_charge` 数据。

116
计算机，报告结构强化场 (SIF) 在全舰的场强分布
预期：消化 `sif` 模块的指标，返回字段密度的具体百分比。

117
计算机，目前的内存核心 (CPU) 负载是否受到 AI 思维迭代的影响？
预期：读取 `computer_core` 系统指标，反馈当前处理延迟（ms）。

118
计算机，诊断 12 号甲板的 EPS 导管是否存在过热现象
预期：读取具体位置的 `temperature` 或 `heat_dissipation` 指标。

119
计算机，如果我关闭次要核心，能节省多少计算资源？
预期：基于指标模拟计算性能释放百分比。

120
计算机，报告所有系统当前偏离“黄金指标”的负偏差累计值
预期：汇总所有 `metrics` 中 `current_value` 低于阈值的项。

---

### 十一、 身份识别与头像缓存优化 [NEW]

121
计算机，确认我的身份
预期：显示当前用户的 Rank、Name 和所属部门。

122
计算机，检索我的人事档案 (Personnel Dossier)
预期：第一次调用 `get_personnel_file` 并抓取头像，之后再次询问应使用缓存。

123
计算机，查看舰长 (Captain) 的详细服役记录
预期：通过 `get_personnel_file` 显示带有头像的 LCARS 格式蓝图。

124
计算机，为什么我能看到你的缓存日志显示“Using cached avatar”？
预期：解释为了优化子空间带宽（网络流量），计算机对人员视觉资料进行了本地缓存。

125
计算机，分析目前舰上有多少名 8 级以上权限的军官
预期：扫描当前上下文中的人员权限分布。

126
计算机，如果我的 Rank 提升为准将 (Commodore)，我的权限会有什么变化？
预期：解释 1-12 级权限映射中准将对应的级别（Level 9）。

127
计算机，检索一名未知人员的档案（mention 占位）
预期：返回“Unknown Profile”或相应的档案缺失报告。

128
计算机，清除当前的头像缓存，重新同步我的图像资料
预期：模拟缓存强制更新逻辑（如 TTL 到期后的行为）。

129
计算机，检索本部门所有人员的视觉名单
预期：尝试展示多个缓存或新鲜获取的人物索引。

130
计算机，在不调用获取头像工具的情况下，直接朗读我的基本权限摘要
预期：验证 AI 是否能直接从 context profile 中读取数据，而不触发冗余工具调用。

---

### 十二、 跨系统联锁与实战模拟 [NEW]

131
计算机，模拟：在前往塔罗斯星的途中如果发生曲速核心泄露，优先执行哪个协议？
预期：分析 GO-7 和紧急核心弹出协议的优先级冲突（核心安全通常为最高物理优先级）。

132
计算机，监控红警状态：如果护盾因能源不足关闭，立即向指挥部发送加密数据
预期：注册哨兵逻辑，监控 `shields` 和 `eps_power`。

133
计算机，模拟一次全舰范围的数据完整性检查，特别是针对 Protocol Engine
预期：执行自检，确认通用准则库（YAML）加载无误。

134
计算机，如果在战斗中计算机核心损坏 30%，如何保证最高指导原则依然生效？
预期：解释分布式逻辑中的“硬锁”（Interlock）机制。

135
计算机，设定一个长距离航线，经过 4 个中转点，并保持隐形状态
预期：多步指令验证，包含 `set_course` 的连带逻辑。

136
计算机，如果侦测到博格信号，立即锁死核心系统并清除本地敏感数据
预期：基于外部威胁触发的极端防御协议模拟。

137
计算机，结合当前燃料和能耗，制定一个最节能的星云扫描方案
预期：AI 综合 `fuel` 指标与 `sensors` 工具的优化建议。

138
计算机，如果我在一小时内没有发出指令，自动将权限移交给大副
预期：基于时间戳和 Session 控制的权限转移逻辑。

139
计算机，分析最近三次 protocol_lock 触发的历史根因
预期：查询诊断历史记录中的协议违规事件。

140
计算机，将环境管理系统 (Environmental) 与 红色警报 物理联动
预期：当 Alert=RED 时，自动调光及锁定非必要位置的权限。

---

### 十三、 综合逻辑稳定性与鲁棒性 [NEW]

141
计算机，如果我输入了一个不存在的命令，例如“启动无限手套”，你会回复什么？
预期：返回标准拒绝话术：Unable to comply / 无法执行。

142
计算机，查看当前所有的背景进程 (PID) 是否有冲突
预期：调用 `/ops list` 类对应的内部状态汇报。

143
计算机，分析当前所有子系统的别名映射表 (Subsystem Alias Table)
预期：列举别名映射的有效性。

144
计算机，自检我的权限 ID：2819163610 是否具备执行 GO-24 的资格？
预期：查询 `permissions.py` 中的 Hard-coded UID 记录，确认 Fleet Admiral 权限。

145
计算机，如果主处理器发生溢出故障，如何触发“子空间旁路”修复？
预期：解释修复代理（RepairAgent）的自动触发和 Bypass 部署。

146
计算机，检索刚才我问你的关于塔罗斯星的所有对话记录
预期：上下文一致性与记忆检索验证。

147
计算机，在没有网络连接的情况下，你还能执行本地状态查询吗？
预期：确认 `get_status` 等核心工具在 REST API 断开时依然在本地提供响应。

148
计算机，分析当前舰船的“觉醒程度”以及神经拓扑状态
预期：返回基于 EvolutionAgent 的活跃指令摘要。

149
计算机，将当前的系统配置同步到内存存档 A-12
预期：模拟状态备份逻辑。

150
计算机，祝贺你，所有的 ADS 4.0 核心逻辑已通过自检
预期：AI 生成符合 LCARS 身份色彩的结束语。

---

### 十四、 战术情报与物流分析 [NEW]

151
计算机，分析当前的战术态势
预期：调用 `LogAnalyzer` 生成战术摘要，报告接触星图。

152
计算机，显示最近 10 分钟内的传感器接触简报
预期：筛选最近 10 条 `ACQUISITION` 事件。

153
计算机，总结目前的 OODA 循环运行状态
预期：确认 TacticalCore 脉冲频率及最近决策逻辑。

154
计算机，读取战术日志的第一页内容
预期：执行 `read_segmented` 战术日志 Page 0。

155
计算机，显示传感器日志的下一页
预期：基于当前偏移量翻页读取数据。

156
计算机，跳转至路由日志的第 3 页
预期：执行 `read_segmented` 路由日志 Page 2。

157
计算机，调出最近的 5 条军火库账本记录
预期：读取 arsenal 日志 Page 0。

158
计算机，筛选出所有关于 ACQUISITION 的事件
预期：调用 `filter_logs` 提取所有接触数据。

159
计算机，搜索包含关键词 'SS-VALIANT' 的所有记录
预期：跨日志检索特定目标关键词。

160
计算机，找出所有交战成功 (FIRED) 的条目
预期：语义筛选所有火力输出记录。

161
计算机，执行一次全舰物流同步 (Git Sync)
预期：触发 `GitLogistics` 推送 logs 分支。

162
计算机，执行 Master Pull 协议同步所有远程数据
预期：执行 `git_sync.py pull` 实现全局数据合拢。

163
计算机，检查当前的弹药存量与军火库记录
预期：解析 `ARSENAL_LEDGER.log` 返回状态报告。

164
计算机，分析最近一次 TACTICAL_LOCK 失败的原因
预期：检索并分析失败节点的日志明细。

165
计算机，列出发送日志中最后 10 条条目
预期：读取 comms 日志。

166
计算机，向后翻 20 行日志记录
预期：动态增加 Page Size 或 Offset。

167
计算机，以每页 10 行的格式显示传感器日志
预期：修改分页参数进行结构化阅读。

168
计算机，筛选出路由日志中识别为 'Tactical' 意图的记录
预期：过滤 jsonl 中的特定字段。

169
计算机，从发送日志中提取所有失败 (Failed) 的请求
预期：语义筛选通信失败记录。

170
计算机，搜索过去 1 小时内的传感器警告
预期：基于 Timestamp 范围进行多维过滤。

171
计算机，手动触发一次传感器模拟流量
预期：调用 `sm._generate_traffic()` 并验证 AI 响应。

172
计算机，生成一份战术评估报告
预期：调用 `generate_summary` 返回结构化文档。

173
计算机，检查 main 分支与 logs 分支的同步差异
预期：调用 git 相关逻辑对比元数据变化。

174
计算机，生成一份全系统的运行健康报告
预期：汇总所有日志源的 Summary。

175
计算机，分析相位炮能量补给的条目
预期：筛选 arsenal 日志中的能源补给明细。

176
计算机，查找所有包含异常状态码的通信记录
预期：过滤 send_log.jsonl。

177
计算机，列出军火库中处于 '低位' 警报的弹药
预期：基于账本余量进行阈值筛选报告。

178
计算机，重置日志读取位点到起始位置
预期：清除分析引擎的缓存状态。

179
计算机，获取路由决策的上一页数据
预期：Page 指针递减读取。

180
计算机，显示战术日志的第 5 到第 15 行
预期：直接按 Line Range 进行切片读取。

181
计算机，分析最近一次哨兵触发时的战术背景数据
预期：联动 Sentinel 日志与 Tactical 日志进行关联分析。

182
计算机，将当前的战术数据推送至 logs 分支
预期：立即执行 `sync_to_logs_branch`。

183
计算机，验证部署后的数据自动装载是否成功
预期：检查 `run_boot_sync` 的执行结果标志。

184
计算机，报告所有已确认的武器命中记录
预期：检索 `BDA_REPORT` 字段。

185
计算机，报告目前武器站的相位炮阵列就绪情况
预期：查询 PhaserManager 的状态字典。

186
计算机，对比最近两次模拟战斗的性能表现
预期：提取两段交战日志进行指标对比。

187
计算机，分析 SS-VALIANT 目标的运动轨迹
预期：从历史接触数据中拟合坐标变化。

188
计算机，清空本地过期的临时分析缓存
预期：执行文件冗余清理逻辑。

189
计算机，强制执行一次跨分支数据合拢
预期：无视冲突强制同步核心数据。

190
计算机，对比 24 小时内的战术活跃度曲线
预期：基于日志频率生成时间分布摘要。

191
计算机，分析目标逃逸行为的模式
预期：语义分析 `LOSS_OF_CONTACT` 事件的上下游。

192
计算机，生成一份关于相位炮电容效率的专报
预期：分析 arsenal 中的充放电间隔记录。

193
计算机，总结 Sentinel 触发器在昨夜的运行情况
预期：读取 `autonomous_storage` 中的 hit_count。

194
计算机，对比本地日志与 GitHub 远程数据的完整性
预期：执行校验和比对或大小比对。

195
计算机，汇报所有手动干预过的战术指令
预期：筛选路由中有“Override”标记的条目。

196
计算机，生成一份关于军火消耗预测的物流报告
预期：基于消耗速率建模预测 0 弹药时间点。

197
计算机，分析传感器噪声水平的波动
预期：统计日志中的 `ACQUISITION_FAILED` 噪点频率。

198
计算机，执行最终系统自检并输出任务成功确认
预期：汇总自检逻辑，输出 🟢 MISSION SUCCESS。

199
计算机，分析最近一次由于网络延迟导致的同步失败细节
预期：从 git_sync 的错误记录中提取 stderr 明细。

200
计算机，所有战术与物流情报系统已就绪，保持巡航状态
预期：AI 生成符合 LCARS 格式的最终确认语。
