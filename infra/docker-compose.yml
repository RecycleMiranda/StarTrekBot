services:
  bot:
    build:
      context: ../services/bot
    env_file:
      - ../services/bot/.env
    environment:
      - NAPCAT_HOST=napcat
      - PROTOCOLS_PATH=/app/data/federation_protocols.json
      - STANDARDS_MD_PATH=/app/data/FEDERATION_STANDARDS.md
      - SOP_CACHE_PATH=/app/data/SOP_CACHE.json
      - GIT_SSH_COMMAND=ssh -o StrictHostKeyChecking=no
    volumes:
      - ..:/app
      - ../data:/app/data
      - ~/.ssh:/root/.ssh:ro
    working_dir: /app/services/bot
    ports:
      - "8080:8080"
    networks:
      - trek-net
    restart: unless-stopped

  napcat:
    image: mlikiowa/napcat-docker:latest
    container_name: napcat
    restart: always
    environment:
      - ACCOUNT=${QQ_ACCOUNT}
    volumes:
      - ./napcat/config:/app/napcat/config
    ports:
      - "6099:6099" # WebUI & Management API
      - "3000:3000" # OneBot 11 HTTP API
    networks:
      - trek-net

networks:
  trek-net:
    driver: bridge
